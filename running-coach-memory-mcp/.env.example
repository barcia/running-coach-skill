# Required
OPENROUTER_API_KEY=sk-or-v1-xxxx

# Optional
DATABASE_PATH=~/.local/share/running-coach/memory.db

# Transport: "stdio" (local, default) or "streamable-http" (remote/VPS)
MCP_TRANSPORT=stdio
MCP_HOST=0.0.0.0
MCP_PORT=8000